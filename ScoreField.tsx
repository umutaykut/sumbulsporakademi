import { requireUser } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { AppShell } from "@/components/AppShell";
export default async function Incidents(){const user=await requireUser(["COORDINATOR","ADMIN"]);const rows=await prisma.incident.findMany({include:{student:{include:{group:true}},reportedBy:true},orderBy:{createdAt:"desc"}});return <AppShell user={user}><div className="topbar"><div><div className="eyebrow">Güvenli takip</div><h1 className="title">Özel durumlar</h1></div></div><div className="card table-wrap"><table className="table"><thead><tr><th>Sporcu</th><th>Grup</th><th>Bildiren</th><th>Açıklama</th><th>Durum</th></tr></thead><tbody>{rows.map(i=><tr key={i.id}><td>{i.student.firstName} {i.student.lastName}</td><td>{i.student.group.name}</td><td>{i.reportedBy.name}</td><td>{i.description}</td><td><span className="badge warn">{i.status}</span></td></tr>)}</tbody></table></div></AppShell>}
